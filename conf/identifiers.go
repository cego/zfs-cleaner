package conf

const (
	planIdentifier    = "plan"
	keepIdentifier    = "keep"
	pathIdentifier    = "path"
	protectIdentifier = "protect"
	includeIdentifier = "include"
)

const (
	keepFor    = "for"
	keepLatest = "latest"
)

const (
	blockStart = "{"
	blockEnd   = "}"
)

const (
	commentBashStyle = '#'
	commentCStyle    = "//"
)
